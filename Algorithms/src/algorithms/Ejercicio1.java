/*
/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Main.java to edit this template
 */


package algorithms;

import java.net.ContentHandlerFactory;
import java.util.Scanner;

/**
 *
 * @author daniel
 */
/*
public class Ejercicio1 {

    /**
     * @param args the command line arguments
     */
/*
    public static void main(String[] args) {
        // TODO code application logic here

        /*
        Crearemos un arreglo de 6 posiciones para almacenar 6 ventas del mes y 
        al final el programa mostrara en pantalla cuales y en total cuantas superan
        la cantida de $2000
        
         */
       /* double ventas[] = new double[6];
        Scanner sc = new Scanner(System.in);
        System.out.println("Dime las ventas del mes de los 6 dias");
        double total = ventas.length;
        for (int i = 0; i < total; i++) {

            System.out.println("Ingrese las ventas del mes" + (i + 1));

            
           ventas[i] = sc.nextDouble();

        }
        int contador = 0;
        for (double elements : ventas) {

            if (elements >= 2000) {

                System.out.println(elements);
                contador++;
            }

            System.out.println(contador + "son mayores a 2000");
        }



       
       double ventas[] = new double[6];
       Scanner sc = new Scanner(System.in);
       double total = ventas.length;
     
       
     for (int i = 0; i < total; i++) {
    
         System.out.println("Dime las ventas :" + (i + 1) );
         
         ventas[i] = sc.nextDouble();
    
}
     
     int k=0;
     int increment=0;
     while (k < 6) {
         
         if (ventas[k] >= 2000) {
             
             System.out.println("las ventas mayores o iguales a 2000 son :" + ventas[k]);
    
             increment++;
}
         k++;
}
   
}
 
       
       
    

}
*/